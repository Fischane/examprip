'use client'

// Minimal toast container — extend with @radix-ui/react-toast if needed
export function Toaster() {
  return <div id="toast-container" className="fixed bottom-4 right-4 z-50 space-y-2" />
}
